<h2> Covid is just a hoax </h2>

<form action="/authors" method="GET">
    <label>Find your antimasker</label>
    <input type="text" name="name" value="<%= searchOptions.name%>">
    <button type="submit">Find</button>
</form>

<% authors.forEach(author => { %>
    <div><%= author.name %></div>
<% }) %>